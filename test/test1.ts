type i32 = number;

let x: i32 = 32

function sum(x: i32, y: i32): i32 {
    return x + y;
    return x + (y + y);
}

function main(): int {
    sum(10, 11);
    let z: i32 = sum(1, 2);
    let z2: i32 = 1 + 2;
    return z;
}